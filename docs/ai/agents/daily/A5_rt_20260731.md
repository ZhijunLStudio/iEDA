# A5 rt · 2026-07-31 · WP-RT-01 起步（规则感知代价 + 强化 min-area）

## 假说

- **H1**：AES@65% 残留 DRC 主因是外环对 short/PRL/min-area **无差别**历史代价，导致 keep-best 与 A* 疏散方向偏弱。
- **H2**：asap7/ics55 的 min-area 可通过提高候选 patch 预算改善，而不必加大 BEST_EFFORT 截断。

## 代码交付（`src/operation/iRT/`）

| 项 | 路径 | 缺省 |
|---|---|---|
| 规则感知权重/代价 | `DRRuleAwareCost.hpp` + `DetailedRouter` / `DRNode` | **OFF** |
| 强化 min-area 候选 | `max_candidate_patch_num` 倍增/boost | **OFF** |
| plateau escalate | `enable_plateau_escalate` / `IEDA_RT_ENABLE_ESCALATION` | **OFF**（修零回归） |
| 无 util 提示时 box | `chooseAdaptiveBoxSize` → **12** | 零回归 |

Config 键：`-enable_rule_aware_cost` / `-enable_enhanced_minarea_repair` / `-enable_plateau_escalate` / `-rule_aware_max_history_scale`
Env：`IEDA_RT_RULE_AWARE_COST` / `IEDA_RT_ENHANCED_MINAREA_REPAIR` / `IEDA_RT_ENABLE_ESCALATION`

## 质量 / 性能门禁

1. **缺省零回归**：新杠杆关 → 违例代价仍为 binary Presence；权重全 1。
2. **A* 热路径**：`_rule_aware_cost_config` 在 `routeDRModel` 缓存一次；`getNodeCost` **不** `getenv`。
3. **代价爆炸封顶**：`rule_aware_max_history_scale` 默认 8。
4. **单元测试**：`irt_dr_rule_aware_cost_test` + `irt_dr_convergence_test` 通过。
5. **附带**：修 `report_evaluator.cpp` 重复 `stage` 声明以恢复链接。

## 杀死实验（下一步 A12）

```bash
# baseline（关）
# A: --rt-rule-aware-cost --rt-enhanced-minarea-repair [--rt-enable-escalation]
# 指标：trueutil 根上 DRC count / EGR WL；标签 proxy
```

## 状态

- WP-RT-01：**doing**（C++ 杠杆已合入源码；待子集 A/B）
- 不算 Done：尚无 AES@65% ≥20% 证据
