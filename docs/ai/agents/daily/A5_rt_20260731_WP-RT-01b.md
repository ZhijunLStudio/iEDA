# A5 rt · 2026-07-31 · WP-RT-01b（冲突分量 escalate + PRL/short repair）

## 假说

- **H1/H2**（01a）：规则感知权重 alone → A/B-1 **未支持**（ΔDRC≈0%，`wp_rt01_ab_20260731_0952`）。
- **H3（本工单）**：plateau 后对**最高 severity 冲突连通分量**做定向 escalate（权重/mild history·violation，**无全局 box×2**）+ short/PRL 优先 rip-up，可在 proxy 预算下相对 A/B-1 baseline 降低 DRC ≥20%。

## 代码交付（`src/operation/iRT/`，缺省 OFF）

| 项 | 路径 / 开关 | 缺省 |
|---|---|---|
| 冲突连通分量 | `DRConflictEscalate.hpp` + `applyComponentEscalateOnPlateau` | **OFF** |
| PRL/short 调度 | `updateTaskSchedule` 按 `prlShortRepairPriority` 排序 | **OFF** |
| 分量权重 boost | `addRouteViolationToGraph` → `componentWeightBoost` | **OFF** |
| mild escalate | `applyComponentMildEscalation`（不 box×2） | 随分量开关 |

Config：`-enable_component_escalate` / `-enable_prl_short_repair` / `-component_escalate_halo_pitch`
Env：`IEDA_RT_COMPONENT_ESCALATE` / `IEDA_RT_PRL_SHORT_REPAIR` / `IEDA_RT_COMPONENT_HALO_PITCH`
Flow：`--rt-component-escalate` / `--rt-prl-short-repair`

## 质量 / 性能门禁

1. **缺省零回归**：两杠杆关 → 调度与权重路径与 01a 关状态一致；无 getenv 进 A*。
2. **禁止**以 MAX_BOXES / BEST_EFFORT 冒充本 WP Done。
3. **单测**：`irt_dr_conflict_escalate_test` + `irt_dr_rule_aware_cost_test` 通过；`bin/iEDA` 已重链。
4. **C-VIO**：A5 ACK `RFC-20260730-C-VIO.md`（内部 `DRConflictViolationRef` 对齐 type/layer/bbox/net_ids/severity）。

## 杀死实验（请 A12 / 本 Agent 后续 A/B-3）

```bash
# baseline：01a 关 + 01b 关（同 proxy 预算）
# treatment：--rt-rule-aware-cost --rt-enhanced-minarea-repair \
#            --rt-component-escalate --rt-prl-short-repair
# 禁止把 MAX_BOXES 变更算进算法 delta
# 标签：proxy；相对 A/B-1 或同批 baseline
```

## 状态

- WP-RT-01b：**代码已合入**；**尚无** AES@65% ≥20% A/B 证据 → 仍 **doing**
- 请 A0/A12 排 A/B-3；请 A1 审缺省关质量（非 ≥20% ACCEPT）
