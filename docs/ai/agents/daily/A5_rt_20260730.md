# A5 rt 日报 · 2026-07-30

## WP-IRT-MAP-01 · 拥塞汇总修复（C-CONG v0）

### 根因（H1 确认）

| 层级 | 问题 |
|---|---|
| **路径** | CSV 写入 `output_path/egr_congestion_map/`（`createDirPath` 在 `feature_path` 空时用 `output_path`），但 `getDefaultOutputPath()` 仅返回 `feature_path` → `eval*Overflow()` 找不到文件 → 返回 **-1** |
| **Sentinel** | `OverflowSummary` 默认 -1；旧版 `PLReporter` 直接打印字符串 |
| **时序** | `report_congestion` 在 route 后执行；CSV 已落盘但 API 查错目录 |

EGR/early-router **map CSV 本身有效**；失效的是汇总 API + 报告接线。

### 交付

1. **C-CONG v0**（C++）
   - `src/evaluation/database/congestion_summary.h`
   - `src/evaluation/src/module/congestion/congestion_summary.cpp`
   - `CongestionAPI::{writeCongestionSummary, readCongestionSummary}`
   - 输出：`{result}/congestion_summary.json`

2. **路径修复**
   - `general_ops.cpp`：`getDefaultOutputPath()` 与 `createDirPath` 对齐
   - `congestion_eval.cpp`：`resolveOverflowMapPath` 增加 `output_path/egr_congestion_map` 候选

3. **报告路径**
   - `report_evaluator.cpp`：写 JSON → 优先读 JSON 填表
   - `PLReporter.cc`：`isValid()` 失败写 N/A（不再泄漏 -1）

4. **Flow / 门禁**
   - `generate_aes11_detailed_report.py`：优先 `congestion_summary.json`；legacy `-1` 仍 invalid
   - `quality_metrics.py`：G5 优先 C-CONG maps

5. **RFC**：`docs/ai/agents/rfc/RFC-20260730-C-CONG-v0.md`

### 离线验证（aes_sky130_a · aes13 trueutil）

union CSV 聚合（应与 JSON `maps.union` 一致）：

| 指标 | 值 |
|---|---|
| bin_count | 22200 |
| total | 194936 |
| max | 76 |
| mean | 8.78 |
| top_1_pct_mean | ~52.4 |
| top_5_pct_mean | ~31.2 |
| nonzero_bin_pct | 47.35% |

`congestion.rpt` 已有 weighted avg **3904.30** / total **194936** / max **76**（trueutil run）。

验证命令：

```bash
# 1) 重编 iEDA 后跑 metrics stage，或单独 report_congestion
# 2) 检查 JSON
cat benchmarks/results/.../workspace/result/congestion_summary.json | jq '.valid,.summary'

# 3) 门禁
python3 benchmarks/qor/quality_metrics.py --result-dir .../workspace/result

# 4) 报告
python3 benchmarks/flows/generate_aes11_detailed_report.py ...
```

### 未做（边界）

- WP-RT-01 plateau DR 外环
- early-router per-layer JSON（仅 EGR place map 聚合）

### 请 A1 审查

- RFC-20260730-C-CONG-v0 schema
- G5 门禁是否改用 `congestion_summary.json` 作为唯一 provenance
