# A6 drc · 2026-07-31 · WP-DRC-01 起步（C-VIO JSON 导出）

## 交付（`src/operation/iDRC/`，缺省 OFF）

| 项 | 说明 |
|---|---|
| `Config.enable_c_vio_json` / `c_vio_json_path` | 缺省 0 / 空 |
| `DRCInterface::outputCVioJson` | `checkDef` 路径；schema `c-vio/v0` |
| Env | `IEDA_DRC_ENABLE_C_VIO=1`；`IEDA_DRC_C_VIO_PATH=<path>` |
| 样例 | `docs/ai/agents/rfc/samples/c_vio_v0_example.json` |

字段对齐 RFC：`type, layer, bbox, net_ids, severity` + `coverage.{checked,skipped,unsupported}`。

## 边界

- **不算** DRC ↓≥20% Done；本轮只冻生产侧契约写出。
- iRT 仍走内存 API；A5 已 ACK 消费侧字段（`DRConflictViolationRef`）。
- 假 clean 不可能：coverage + violation total 同写。

## 下一步

1. Tcl `init_drc` 透传 `-enable_c_vio_json`（可选）
2. 注入 short 必检出实验
3. 与 A5 联调：plateau 后读 `c_vio.json`（可选，非热路径）

日报对应 RFC ACK：A6 **ACK（生产侧）**。
