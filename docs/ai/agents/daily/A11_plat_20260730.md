# A11 plat — WP-PLT-01 审计（2026-07-30）

## 结论

`DesignState` / `DirtySet` / `MoveTxn` **已存在**于 `src/platform/design_state/`，并有 `tests/DesignStateTest.cpp`。
`DataManager::placeInst` 已可接入 `MoveTxn`。

## 现状 vs Wave-2 缺口

| 能力 | 状态 |
|---|---|
| MoveTxn commit/rollback | 有 |
| DirtySet merge | 有 |
| iTO/iCTS 默认走 MoveTxn | **未接线**（Wave-2） |
| ArtifactIndex sha256 门禁 | 部分（flow summary） |
| 缺 SPEF / residual DRC 硬失败 | flow 已有 `--require-spef` / `--rt-fail-on-residual-drc`（默认关） |

## 本轮交付

- 审计文档（本文件）
- RFC：`docs/ai/agents/rfc/RFC-20260730-C-TXN.md`（若未写则沿用平台头文件为契约）
- **不重复实现** MoveTxn 内核

## 建议 A1 Verdict

WP-PLT-01 骨架：**proxy/done 可条件接受**；Wave-2 前必须完成 iTO 真 apply 接线。
