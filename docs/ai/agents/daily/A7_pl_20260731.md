# A7 pl · 2026-07-31 · WP-PL-01 起步（缺省 OFF + density_scale 回退）

## 假说

- **H-PL-1**：RUDY util → `density_scale` inflation 可降 EGR overflow / 下游 DRC；但缺省 ON 会改变非 aes 基线。
- **H-PL-2**：发散时只回滚坐标不回滚 `density_scale` 会留下膨胀残留 → 加回退可稳 QoR。

## 代码交付（`src/operation/iPL/`）

| 项 | 改动 | 缺省 |
|---|---|---|
| `pl_default_config.json` | `is_congestion_effort` **1→0** | **OFF**（与 aes workspace 对齐） |
| `NesterovPlace.cc` | keep-best 时同步快照/回滚 `density_scale` | 仅 congestion ON 时生效 |

开关仍为 `PL.is_congestion_effort` / TCL `-is_congestion_effort`。E-PL-01 A/B：显式打开后对照 trueutil proxy。

## 状态

- WP-PL-01：**doing**（杠杆纪律 + 回退合入；尚无 EGR A/B）
- 不算 Done：无 ≥20% EGR/overflow 证据
