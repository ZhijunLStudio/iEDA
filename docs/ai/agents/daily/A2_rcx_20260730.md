# A2 rcx 日报 — 2026-07-30

## Done

- **PDK 资产盘点**（`/home/lxq/AiEDA/Foundary`）：全树 `*.itf` / `*.captab` **零文件**；sky130 仅有 Innovus 产物 `sky130/spef/gcd.spef`（proxy，非 iRCX）；nangate45 有 `setRC.tcl` / `rcx_patterns.rules`（OpenRCX 格式，**iRCX 不可用**）。
- **`aes13_flow.py` 接线**：routing 与 timing 之间插入 `rcx` stage；workspace 生成 `iEDA_config/rcx_config.json`、`rcx_layer_mapping.txt`、`script/custom/run_rcx.tcl`；timing/power 在 `SPEF_FILE` 存在时 `db_init_spef`，`--require-spef` 时缺失非零退出。
- **Layer mapping 模板**：`benchmarks/flows/rcx/sky130|nangate45/layer_mapping.txt`（identity mapping，待 ITF 对齐）。
- **契约草案**：`docs/ai/agents/rfc/RFC-20260730-C-SPEF-v0.md`（`c-spef/v0` + `rcx_coverage.json`）。

## Delta

| 文件 | 变更 |
|---|---|
| `benchmarks/flows/aes13_flow.py` | RCX stage、SPEF 回标、CLI、`rcx_coverage.json` |
| `benchmarks/flows/rcx/sky130/layer_mapping.txt` | 新增 |
| `benchmarks/flows/rcx/nangate45/layer_mapping.txt` | 新增 |
| `docs/ai/agents/rfc/RFC-20260730-C-SPEF-v0.md` | 新增 |

## Blocked

- **B-RCX-ITF**（未解除）：需 PDK 侧 ITF+captab 落盘至例如 `Foundary/sky130/rcx/{sky130.itf,sky130.captab}`；当前 `init_rcx` 会在空路径失败，flow 默认 **soft-skip rcx**（除非 `--require-spef`）。
- **G8 / trusted WNS**：未宣称；`--fallback-spef` 仅用于 iSTA 读链 smoke（gcd≠aes，C_UNIT=PF）。

## Next

1. PDK/Foundary 合入 sky130 ITF+captab 后：`--design aes_sky130_a --stop-after rcx --require-spef` 验证 `result/rcx/*.spef`。
2. 与 A3 联调：`--fallback-spef` daily 子集确认 `read_spef` + net delay 非零（标 `proxy`）。
3. A1 审查 `rcx_coverage.json` 字段与 C-SPEF v0 ACK。

### Daily 子集验证命令（不跑 12 设计）

```bash
# 1) 仅准备 workspace（检查 rcx 配置产物）
python3 benchmarks/flows/aes13_flow.py \
  --design aes_sky130_a aes_nangate45_a \
  --no-synthesis --prepare-only

# 2) 已有 route DEF 时 resume 到 rcx（ITF 到位后）
python3 benchmarks/flows/aes13_flow.py \
  --design aes_sky130_a --no-synthesis --resume --stop-after rcx

# 3) Wave-0 强制门禁（ITF 未就绪会 fail）
python3 benchmarks/flows/aes13_flow.py \
  --design aes_sky130_a --no-synthesis --resume --require-spef --stop-after timing

# 4) iSTA 读链 smoke（proxy SPEF，非 signoff）
python3 benchmarks/flows/aes13_flow.py \
  --design aes_sky130_a --no-synthesis --resume --skip-rcx \
  --fallback-spef --stop-after timing
```
